name: Gunluk BES Raporu Otomasyonu

on:
  schedule:
    - cron: '30 16 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Depoyu Klonla
      uses: actions/checkout@v3

    - name: Python Kurulumu
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Temel Kutuphaneleri Yukle
      run: |
        python -m pip install --upgrade pip
        pip install pandas openpyxl requests tefas tefas-crawler

    - name: Raporu Calistir
      run: python main.py

    - name: Olusan Excel Dosyasini GitHub'a Yukle
      uses: actions/upload-artifact@v4
      with:
        name: Gunluk-BES-Excel-Raporu
        path: "*.xlsx"
